# Docker Compose / PostgreSQL container
POSTGRES_PORT=5432
POSTGRES_DB=simple_invoice
POSTGRES_USER=simple_invoice_user
POSTGRES_PASSWORD=change-me-local-dev-password

# Exposed host ports
APP_PORT=4000
FRONTEND_PORT=3000
