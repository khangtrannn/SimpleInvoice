import {
  createMockAuthUser,
  createMockLoginRequest,
} from '@/test/factories/auth.factory';

export const reviewerUser = createMockAuthUser();

export const reviewerCredentials = createMockLoginRequest();
